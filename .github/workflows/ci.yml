name: Genicons CI

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        python-version: [3.8]
        poetry-version: [1.1.12]

    services:
      db:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root_password
          MYSQL_DATABASE: sample_db
          MYSQL_USER: user
          MYSQL_PASSWORD: password
        ports:
          - 3306:3306
        options: >-
          --health-cmd "mysqladmin ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install Poetry
        uses: abatilo/actions-poetry@v2.1.3
        with:
          poetry-version: ${{ matrix.poetry-version }}
      - name: Poetry install dependencies
        run: |
          cd api
          poetry install --no-interaction
      - name: Lint with flake8
        run: |
          cd api
          poetry run flake8 ./app --count --show-source --statistics --exit-zero
      - name: Check MySQL Container
        run: |
          docker ps -a
          mysql --protocol=tcp -h localhost -P 3306 -u root -proot_password -e "SHOW DATABASES;"
          mysql --protocol=tcp -h localhost -P 3306 -u user -ppassword -e "SHOW DATABASES;"
      - name: Test with pytest
        run: |
          cd api
          poetry run pytest --rootdir=./app/test/ -c ./app/test/pytest.ini
